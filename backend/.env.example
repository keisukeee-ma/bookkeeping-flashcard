# バックエンド環境変数

# PostgreSQL接続URL（Supabase等）。未設定の場合はローカルのSQLiteを使用
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# フロントエンドのURL（CORS許可用）。Vercelのデプロイ先URLを設定
# FRONTEND_URL=https://bookkeeping-flashcard.vercel.app
